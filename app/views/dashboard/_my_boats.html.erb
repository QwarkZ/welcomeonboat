<div class="container">
  <div class="cards">
  <% @my_boats.each do |boat| %>
    <div class="card-boat">
      <%= cl_image_tag boat.photo.key, height: 300, width: 400, crop: :fill %>
      <div class="card-boat-infos">
        <div>
          <p>📍<%= boat.address %></p>
          <h2><%= category(boat.category)%><%= boat.title %></h2>
        </div>
        <h2 class="card-boat-pricing pt-2"><%= boat.price_per_day %> € <span>/ jour</span></h2>
        <%= cl_image_tag boat.owner.photo.key, crop: :fill, class: "avatar card-boat-user avatar-bordered" %>
      </div>
    </div>
  <% end %>
  </div>
  <div>
    <%= link_to "Ajouter un bateau", class: "btn btn-add-boat mt-4 mb-4" %>
  </div>
</div>